# Uncomment to suppress warnings coming from external libraries.
#
#cxx.internal.scope = current

# No MoltenVK public headers that pull broken Apple SDK paths under later
# language standards, so latest is fine here (libmoltenvk pins to 17).
#
cxx.std = latest

using cxx
using cxx.objcxx # OSSupport.mm is Objective-C++.

assert ($cxx.target.class == 'macos') \
  'moltenvk-shader-converter is macOS-only'

assert ($cxx.id.type == 'clang') \
  'moltenvk-shader-converter requires clang (Objective-C++ and Metal frameworks)'

hxx{*}: extension = hpp
ixx{*}: extension = ipp
txx{*}: extension = tpp
cxx{*}: extension = cpp
h{*}:   extension = h
